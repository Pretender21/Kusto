// NetworkLocation parser
| mv-apply todynamic(NetworkLocationDetails) on (
    where isnotempty(NetworkLocationDetails.networkNames)
    | summarize
        NetLocations = make_set(NetworkLocationDetails.networkNames)
    )
